[package]
name = "srv"
version = "0.1.0"
edition = "2024"

[dependencies]
httparse = "1"

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
